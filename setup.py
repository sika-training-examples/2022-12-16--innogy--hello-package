from setuptools import setup

setup(
   name='hello',
   version='1.0.0',
   description='Hello World Example',
   author='Ondrej Sika',
   author_email='ondrej@ondrejsika.com',
   packages=['hello'],
   install_requires=[],
)
